<h1>
    index blade
</h1>


{{-- @foreach ($hobbies as $hobby )
    @foreach ($hobbies as $hobby )
        {{$loop->depth}}
        {{$loop->parent->depth}}
    @endforeach
@endforeach --}}

{{-- @include('shared.button', ['color' => 'red', 'text' => 'submit']) --}}

{{-- @php
$cars  = [1,2,3,4,5];
@endphp

@each('car.view', $cars, 'car', 'car.empty') --}}

